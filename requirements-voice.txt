# Голос — необязательная часть моста. Без этих двух строк он работает текстом
# и на голосовое честно просит повторить словами.
#
# Ставится так:  bash scripts/setup.sh --voice
# Весит немало: модель распознавания «small» — около 500 МБ на диске и до 800 МБ
# памяти на время расшифровки, голос piper — ещё 63 МБ.

faster-whisper>=1.0      # распознавание речи на самой машине, без ключей и облаков
piper-tts>=1.2           # чтение вслух на самой машине; голос лежит файлом .onnx
